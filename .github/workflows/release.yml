name: Release

run-name: Release ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing annotated v<SemVer> tag to publish
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  package:
    name: Validate, build, and package
    runs-on: windows-latest
    timeout-minutes: 40
    outputs:
      tag: ${{ steps.version.outputs.tag }}
      version: ${{ steps.version.outputs.version }}
      file_version: ${{ steps.version.outputs.file_version }}
      prerelease: ${{ steps.version.outputs.prerelease }}
      artifact_name: replica-release-${{ steps.version.outputs.version }}

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_NOLOGO: true

    steps:
      - name: Check out release tag
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

      - name: Validate tag and project version
        id: version
        shell: powershell
        env:
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
        run: ./scripts/validate-release-version.ps1 -Tag $env:RELEASE_TAG -RequireGitTag -GitHubOutputPath $env:GITHUB_OUTPUT

      - name: Set up .NET 10
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: |
            global.json
            Directory.Packages.props
            **/*.csproj

      - name: Restore
        run: dotnet restore Replica.sln

      - name: Verify formatting
        run: dotnet format Replica.sln --verify-no-changes --no-restore

      - name: Build Release
        run: dotnet build Replica.sln -c Release --no-restore -p:Version=${{ steps.version.outputs.version }} -p:FileVersion=${{ steps.version.outputs.file_version }} -p:AssemblyVersion=${{ steps.version.outputs.file_version }} -p:InformationalVersion=${{ steps.version.outputs.version }}

      - name: Test
        run: dotnet test Replica.sln -c Release --no-build --logger "trx;LogFilePrefix=replica-release" --results-directory TestResults

      - name: Audit vulnerable packages
        run: dotnet list Replica.sln package --vulnerable --include-transitive --no-restore

      - name: Install Inno Setup 6.7.1
        run: choco install innosetup --version=6.7.1 --yes --no-progress

      - name: Publish self-contained application and build installer
        shell: powershell
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: ./scripts/build-release.ps1 -Version $env:RELEASE_VERSION -SkipVerification

      - name: Prepare canonical GitHub Release Assets
        shell: powershell
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: ./scripts/prepare-release-assets.ps1 -Version $env:RELEASE_VERSION -ReleaseDirectory artifacts/release -OutputDirectory artifacts/release-package

      - name: Upload release package
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: replica-release-${{ steps.version.outputs.version }}
          path: |
            artifacts/release-package/ReplicaSetup-${{ steps.version.outputs.version }}.exe
            artifacts/release-package/ReplicaSetup-${{ steps.version.outputs.version }}.exe.sha256
            artifacts/release-package/ReplicaSetup.exe
            artifacts/release-package/ReplicaSetup.exe.sha256
          if-no-files-found: error
          retention-days: 7
          compression-level: 0

      - name: Upload release test results
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: release-test-results-${{ steps.version.outputs.version || github.run_id }}
          path: TestResults/**/*.trx
          if-no-files-found: warn
          retention-days: 7

  package-macos:
    name: Package macOS (${{ matrix.architecture }})
    runs-on: macos-latest
    timeout-minutes: 40
    strategy:
      fail-fast: false
      matrix:
        architecture:
          - arm64
          - x64

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_NOLOGO: true

    steps:
      - name: Check out release tag
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

      - name: Validate tag and project version
        id: version
        shell: pwsh
        env:
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
        run: ./scripts/validate-release-version.ps1 -Tag $env:RELEASE_TAG -RequireGitTag -GitHubOutputPath $env:GITHUB_OUTPUT

      - name: Set up .NET 10
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: |
            global.json
            Directory.Packages.props
            src/Replica.Core/Replica.Core.csproj
            src/Replica.Storage/Replica.Storage.csproj
            src/Replica.Mac.Infrastructure/Replica.Mac.Infrastructure.csproj
            src/Replica.Mac/Replica.Mac.csproj
            tests/Replica.Mac.Tests/Replica.Mac.Tests.csproj

      - name: Restore macOS projects
        run: dotnet restore tests/Replica.Mac.Tests/Replica.Mac.Tests.csproj

      - name: Build and test macOS projects
        shell: pwsh
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: |
          dotnet build tests/Replica.Mac.Tests/Replica.Mac.Tests.csproj -c Release --no-restore -p:Version=$env:RELEASE_VERSION -p:InformationalVersion=$env:RELEASE_VERSION
          if ($LASTEXITCODE -ne 0) { throw 'macOS build failed.' }
          dotnet test tests/Replica.Mac.Tests/Replica.Mac.Tests.csproj -c Release --no-build
          if ($LASTEXITCODE -ne 0) { throw 'macOS tests failed.' }

      - name: Create self-contained app bundle and DMG
        shell: pwsh
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: ./scripts/build-macos-release.ps1 -Version $env:RELEASE_VERSION -Architecture ${{ matrix.architecture }}

      - name: Upload macOS release package
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: replica-macos-${{ steps.version.outputs.version }}-${{ matrix.architecture }}
          path: |
            artifacts/macos-release/Replica-macOS-${{ matrix.architecture }}.dmg
            artifacts/macos-release/Replica-macOS-${{ matrix.architecture }}.dmg.sha256
          if-no-files-found: error
          retention-days: 7
          compression-level: 0

  attest:
    name: Attest installer provenance
    if: github.event.repository.visibility == 'public'
    needs:
      - package
      - package-macos
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      actions: read
      id-token: write
      attestations: write

    steps:
      - name: Download release package
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: ${{ needs.package.outputs.artifact_name }}
          path: release-package

      - name: Download macOS release packages
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          pattern: replica-macos-${{ needs.package.outputs.version }}-*
          path: release-package
          merge-multiple: true

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@43d14bc2b83dec42d39ecae14e916627a18bb661 # v3
        with:
          subject-path: release-package/*

  release:
    name: Publish GitHub Release
    if: always() && needs.package.result == 'success' && needs.package-macos.result == 'success' && (needs.attest.result == 'success' || needs.attest.result == 'skipped')
    needs:
      - package
      - package-macos
      - attest
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: read
      contents: write

    steps:
      - name: Download verified release package
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: ${{ needs.package.outputs.artifact_name }}
          path: release-package

      - name: Download verified macOS packages
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          pattern: replica-macos-${{ needs.package.outputs.version }}-*
          path: release-package
          merge-multiple: true

      - name: Verify installer SHA-256
        shell: pwsh
        run: |
          $expected = (Get-Content -LiteralPath release-package/ReplicaSetup.exe.sha256 -Raw).Trim().Split(' ', [StringSplitOptions]::RemoveEmptyEntries)[0]
          $actual = (Get-FileHash -LiteralPath release-package/ReplicaSetup.exe -Algorithm SHA256).Hash
          if (!$actual.Equals($expected, [StringComparison]::OrdinalIgnoreCase)) {
            throw 'ReplicaSetup.exe failed SHA-256 verification before publication.'
          }
          foreach ($architecture in @('arm64', 'x64')) {
            $name = "Replica-macOS-$architecture.dmg"
            $checksum = "release-package/$name.sha256"
            $expected = (Get-Content -LiteralPath $checksum -Raw).Trim().Split(' ', [StringSplitOptions]::RemoveEmptyEntries)[0]
            $actual = (Get-FileHash -LiteralPath "release-package/$name" -Algorithm SHA256).Hash
            if (!$actual.Equals($expected, [StringComparison]::OrdinalIgnoreCase)) {
              throw "$name failed SHA-256 verification before publication."
            }
          }

      - name: Create immutable GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.package.outputs.tag }}
          RELEASE_VERSION: ${{ needs.package.outputs.version }}
          RELEASE_PRERELEASE: ${{ needs.package.outputs.prerelease }}
        run: |
          $arguments = @(
            'release', 'create', $env:RELEASE_TAG,
            '--repo', $env:GITHUB_REPOSITORY,
            '--verify-tag',
            '--title', "Replica $env:RELEASE_VERSION",
            '--generate-notes',
            '--notes', "> [!WARNING]`n> The Windows installer is **Unsigned**. The macOS apps are ad-hoc signed and **not notarized**. Verify the matching SHA-256 file before opening a download.",
            'release-package/ReplicaSetup.exe',
            'release-package/ReplicaSetup.exe.sha256',
            'release-package/Replica-macOS-arm64.dmg',
            'release-package/Replica-macOS-arm64.dmg.sha256',
            'release-package/Replica-macOS-x64.dmg',
            'release-package/Replica-macOS-x64.dmg.sha256')
          if ($env:RELEASE_PRERELEASE -eq 'true') {
            $arguments += '--prerelease'
          }
          & gh @arguments
          if ($LASTEXITCODE -ne 0) {
            throw "GitHub Release creation failed with exit code $LASTEXITCODE."
          }
