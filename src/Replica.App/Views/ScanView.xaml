<UserControl x:Class="Replica.App.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.Name="현재 PC 읽기 전용 스캔">
    <ScrollViewer Padding="32,26" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="1120">
            <Border Style="{DynamicResource PageCardStyle}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock AutomationProperties.HeadingLevel="Level2" FontSize="20" FontWeight="SemiBold" Text="단계별 진행률" />
                            <TextBlock Margin="0,6,0,0" AutomationProperties.LiveSetting="Polite" FontWeight="SemiBold" Text="{Binding ScanCurrentStage, StringFormat=현재 단계: {0}}" />
                            <TextBlock Margin="0,3,0,0" Foreground="{DynamicResource SecondaryTextBrush}" Text="{Binding ScanStatus}" TextWrapping="Wrap" />
                        </StackPanel>
                        <WrapPanel Grid.Column="1">
                            <Button Margin="0,0,8,0" Padding="14,8" AutomationProperties.Name="스캔 시작" Command="{Binding ScanCurrentComputerCommand}" Content="_스캔 시작" />
                            <Button Padding="14,8" AutomationProperties.Name="스캔 취소" Command="{Binding ScanCurrentComputerCancelCommand}" Content="_취소" />
                        </WrapPanel>
                    </Grid>
                    <ProgressBar Margin="0,16,0,0" Height="12" Minimum="0" Maximum="100" Value="{Binding ScanProgressPercentage}" AutomationProperties.Name="스캔 진행률" />
                    <TextBlock Margin="0,6,0,0" HorizontalAlignment="Right" Text="{Binding ScanProgressPercentage, StringFormat={}{0}%}" />
                </StackPanel>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="16" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{DynamicResource PageCardStyle}">
                    <StackPanel>
                        <TextBlock AutomationProperties.HeadingLevel="Level2" FontSize="18" FontWeight="SemiBold" Text="발견 항목" />
                        <DataGrid Margin="0,12,0,0" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding ScanFoundItems}" AutomationProperties.Name="스캔 발견 항목">
                            <DataGrid.Columns>
                                <DataGridTextColumn Width="130" Header="범주" Binding="{Binding Category}" />
                                <DataGridTextColumn Width="80" Header="개수" Binding="{Binding Count}" />
                                <DataGridTextColumn Width="*" Header="상세" Binding="{Binding Detail}" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{DynamicResource PageCardStyle}">
                    <StackPanel>
                        <TextBlock AutomationProperties.HeadingLevel="Level2" FontSize="18" FontWeight="SemiBold" Text="경고" />
                        <TextBlock Margin="0,5,0,0" Foreground="{DynamicResource SecondaryTextBrush}" Text="Partial Failure는 성공한 결과를 유지하고 명시적으로 표시합니다." TextWrapping="Wrap" />
                        <DataGrid Margin="0,12,0,0" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding ScanWarnings}" AutomationProperties.Name="스캔 경고 목록">
                            <DataGrid.Columns>
                                <DataGridTextColumn Width="110" Header="Provider" Binding="{Binding Provider}" />
                                <DataGridTextColumn Width="110" Header="Code" Binding="{Binding Code}" />
                                <DataGridTextColumn Width="*" Header="메시지" Binding="{Binding Message}" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{DynamicResource PageCardStyle}">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="완료 요약" />
                    <TextBlock Margin="0,8,0,0" Text="{Binding ScanSummaryText}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
